name: StudyBuddy CI/CD

on:
  push:
    branches:
      - main

jobs:
  cicd-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Set up Python3
        uses: actions/setup-python@v2
        with:
          python-version: 3.11.4

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Initialize Static Files
        run: python manage.py collectstatic

      - name: Linter Verification
        run: pylint StudyBuddy

      - name: Deploy Service
        run: curl "https://api.vercel.com/v1/integrations/deploy/prj_ibtGj3zSE6z6VqtALZ5Xj4Sq3jYY/${{ secrets.VERCEL_API_KEY }}"
